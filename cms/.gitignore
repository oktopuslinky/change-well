node_modules
.next
build
dist

# env / secrets — never commit
.env
.env.*
!.env.example

# local sqlite database + uploaded media
*.db
*.db-journal
*.sqlite
/media
/uploads

# payload generated (regenerated by scripts)
# src/payload-types.ts  # keep committed if you prefer

.DS_Store
*.log
